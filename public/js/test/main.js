var Character = require('../main');
var chai = require('chai');
var expect = chai.expect;

describe('Character', function() {
  describe('#characterSearch()', function() {
    context('when search is Tacopies', function() {
      it('returns character info', function() {
        expect(result).to.equal(payload); // probably going to need to define exactly the object returned.
      });
    });
    // context('when function ')
  });
});
